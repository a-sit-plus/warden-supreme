---
hide:
  - toc
---

# {{ module_title }} SBOM

The table below lists all SBOM publications for this module.

- `metadata` refers to the root `kotlinMultiplatform` publication
- `target` refers to a concrete published target artifact such as `jvm`, `android`, `iosArm64`, or `iosSimulatorArm64`
- `mavenJava` is the published JVM artifact for server-side modules

Use the JSON/XML links for the exact CycloneDX SBOM attached to that publication.

| Publication | Type | Coordinates | Artifact | JSON SBOM | XML SBOM |
| --- | --- | --- | --- | --- | --- |
{{ table_rows }}
